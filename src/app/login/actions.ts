'use server';

// Sign-up action has been removed (Story 1.2 AC5: Admin self-registration vulnerability closed).
// Admin users are created only via existing admin or database seed.
// Authentication is now handled client-side via Supabase Auth.
