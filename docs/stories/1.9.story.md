# Story 1.9: Firebase Cleanup + Deployment

**Epic:** EPIC-1 (Firebase → Supabase Quality Migration)
**Sprint:** 3 (Day 20)
**Effort:** 1 day
**Priority:** P1
**Status:** InReview
**Agent:** @dev (Dex) + @devops (Gage)
**Blocked By:** Story 1.8 (QA gate must PASS)
**Blocks:** Story 1.10

---

## Story

**As a** developer,
**I want** all Firebase dependencies removed and the application deployed to the production hosting platform,
**So that** the codebase is clean and the migration is ready for go-live.

---

## Context

After QA gate passes (Story 1.8), this story removes all Firebase code, packages, and configuration. The application is deployed to the chosen hosting platform (pending decision D4, recommended: Vercel). Firebase is NOT decommissioned yet — it remains as a rollback target for 2 weeks.

**Reference:** `docs/architecture/technical-debt-assessment.md` §3.3 Phase D, §7 Rollback Strategy

---

## Acceptance Criteria

- [x] **AC1:** All Firebase packages removed
  - `firebase` and `firebase-admin` removed from package.json
  - `genkit-cli` removed (dead code, referenced removed AI flows)
  - 0 Firebase imports in any source file (verified via grep)
  - `npm install` succeeds without Firebase deps

- [x] **AC2:** Firebase source files removed
  - `src/firebase/` directory removed (10 files: provider, hooks, errors, config)
  - `src/lib/firebase/` directory removed (3 files: client, server, config)
  - `functions/` directory removed (Cloud Functions)
  - `src/components/FirebaseErrorListener.tsx` removed
  - Firebase config files removed: `.firebaserc`, `firebase.json`, `firestore.rules`
  - Profit-stats page converted from Firebase to local-state-only (EPIC-2 Story 2.7 will add Supabase persistence)

- [x] **AC3:** Environment variables updated
  - No Firebase env vars in `.env.example`
  - `.env` is gitignored (owner to remove Firebase vars manually)
  - Supabase env vars already configured

- [x] **AC4:** Package cleanup (LOW items)
  - Package name fixed: "nextn" → "mistertuga" (TD-L1)
  - Empty AI flows removed: `src/ai/` directory deleted (TD-L2)
  - Placeholder images utility removed: `src/lib/placeholder-images.{ts,json}` deleted (TD-L3)
  - Genkit scripts removed from package.json
  - NOTE: TD-L6 (shadows), TD-L7 (memoization), TD-L8 (Supabase link) deferred — low priority, not blocking

- [x] **AC5:** Application build verified
  - `npm run build` passes (all pages compile)
  - `npm run typecheck` passes (0 errors)
  - Bundle improvement: profit-stats First Load JS 323 kB → 201 kB (-122 kB)
  - NOTE: Deployment to Vercel is a manual step (owner @devops)

- [x] **AC6:** Firebase NOT decommissioned
  - Firebase project `gen-lang-client-0378123449` remains active
  - Firestore data intact (576 orders, 563 customers)
  - Firebase can serve as rollback target for 2 weeks

---

## Tasks

- [x] 1. Remove `firebase`, `firebase-admin`, `genkit-cli` from package.json
- [x] 2. Delete `src/firebase/` directory (10 files)
- [x] 3. Delete `src/lib/firebase/` directory (3 files)
- [x] 4. Delete `functions/` directory (Cloud Functions)
- [x] 5. Delete Firebase config files (.firebaserc, firebase.json, firestore.rules)
- [x] 6. Delete `src/components/FirebaseErrorListener.tsx`
- [x] 7. Convert profit-stats from Firebase to local-state-only
- [x] 8. Remove FirebaseClientProvider from app layout
- [ ] 9. Remove Firebase env vars from `.env` — **manual: owner**
- [x] 10. Fix package name: "nextn" → "mistertuga" (TD-L1)
- [x] 11. Remove empty AI flows: `src/ai/` directory (TD-L2)
- [x] 12. Remove placeholder images: `src/lib/placeholder-images.*` (TD-L3)
- [x] 13. Remove genkit scripts from package.json
- [x] 14. Run `npm run typecheck` — 0 errors
- [x] 15. Run `npm run build` — success, all pages compile
- [ ] 16. Configure Vercel hosting — **manual: @devops**
- [ ] 17. Set Supabase env vars in Vercel — **manual: @devops**
- [ ] 18. Deploy to production — **manual: @devops**
- [ ] 19. Verify deployed application works — **manual**
- [ ] 20. Verify Firebase still accessible (rollback target) — **manual**

---

## Debt Items Resolved

| ID | Description | Severity | Resolution |
|----|-------------|----------|------------|
| TD-L1 | Package name mismatch | LOW | Fixed to "mistertuga" |
| TD-L2 | Empty AI flows file | LOW | Removed |
| TD-L3 | Placeholder images dead code | LOW | Removed |
| TD-L6 | Custom shadows not tokenized | LOW | Tokenized |
| TD-L7 | No memoization | LOW | React.memo on heavy components |
| TD-L8 | Supabase linked but unused | LOW | Resolved (now used) |
| TD-X1 | Login gradient difference | COSMETIC | Intentional (documented) |

---

## Testing

- `npm run build` passes without errors
- `npm run lint` passes
- `npm run typecheck` passes
- Deployed URL loads and functions correctly
- No Firebase references in deployed bundle (check source maps)
- Firebase Hosting still accessible at original URL

---

## File List

| File | Action | Notes |
|------|--------|-------|
| `src/firebase/` | Deleted | 10 files: config, provider, hooks, errors |
| `src/lib/firebase/` | Deleted | 3 files: client, server, config |
| `src/components/FirebaseErrorListener.tsx` | Deleted | Error listener component |
| `functions/` | Deleted | Cloud Functions directory |
| `.firebaserc` | Deleted | Firebase CLI config |
| `firebase.json` | Deleted | Firebase project config |
| `firestore.rules` | Deleted | Firestore security rules |
| `src/ai/` | Deleted | Empty AI flows (TD-L2) |
| `src/lib/placeholder-images.ts` | Deleted | Dead code (TD-L3) |
| `src/lib/placeholder-images.json` | Deleted | Empty data (TD-L3) |
| `package.json` | Modified | Name: nextn→mistertuga, removed Firebase/genkit deps+scripts |
| `package-lock.json` | Modified | Dependency tree updated |
| `src/app/(app)/layout.tsx` | Modified | Removed FirebaseClientProvider wrapper |
| `src/app/(app)/profit-stats/page.tsx` | Modified | Firebase→local-state-only |
| `docs/stories/1.9.story.md` | Modified | Story progress |

---

## Dev Notes

- **Profit-stats converted to local-state-only**: Renders dummy data, extras work in-session but don't persist. EPIC-2 Story 2.7 will add Supabase persistence.
- **Bundle improvement**: profit-stats First Load JS dropped 323 kB → 201 kB (-37%) by removing Firebase SDK.
- **Cloud Function replaced**: Story 1.7 created the Supabase `notify_tracking_added()` trigger as replacement. The Firebase Cloud Function is no longer needed.
- **TD-L6, L7, L8 deferred**: Custom shadows, memoization, and Supabase link cleanup are low-priority cosmetic items. Not blocking deployment.
- **ESLint not configured**: Next.js 14 / ESLint 8-9 version conflict. Deferred to post-EPIC-1.
- **TypeScript**: Clean (0 errors). **Build**: Clean (all pages compile).

---

## QA Results

*Updated by @qa*

---

## Change Log

| Date | Change | Author |
|------|--------|--------|
| 2026-03-02 | Story created from Brownfield Discovery Phase 10 | @pm (Morgan) |
| 2026-03-03 | Firebase fully removed. 15+ files deleted, 3 packages removed, profit-stats converted to local-state. Build clean, bundle improved. Manual deployment steps remain. | @dev (Dex) |
